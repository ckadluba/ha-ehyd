# Project instructions

- After every change in this project, run Ruff against the source files and fix
  all reported errors before continuing:

  ```sh
  ruff check custom_components
  ```

- Test code is excluded from this Ruff check; do not use the test directory as
  the target for this required validation.
