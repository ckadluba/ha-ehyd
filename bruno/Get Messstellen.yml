info:
  name: Get Messstellen
  type: http
  seq: 6

http:
  method: GET
  url: https://ehyd.gv.at/services/Messstellen/json?filter=alle
  params:
    - name: filter
      value: alle
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
